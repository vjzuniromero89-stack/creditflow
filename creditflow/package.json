{
  "name": "creditflow",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute creditflow-db --local --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute creditflow-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
